Para compilar abra um terminal, navegue até a pasta Ram e execute o arquivo "Makefile" com o comando "make".

Para executar abra um terminal, navegue até a pasta Ram e execute o arquivo "Ram" com o comando "./Ram".
